/**
 * given:
 * - a path to a config which specifies the root of the project to check
 * - a path to the declarations directory
 *
 * does:
 * - check that the project conforms to the practices specified in the config
 */
export const checkProject = async () => {};
