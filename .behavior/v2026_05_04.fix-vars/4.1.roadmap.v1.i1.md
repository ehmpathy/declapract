# roadmap: array variable support

## overview

single phase execution. the blueprint is minimal — one function modification and one test addition.

---

## phase 0: implementation

### prereqs

read before execution:
- `.behavior/v2026_05_04.fix-vars/3.3.1.blueprint.product.v1.i1.md` — the blueprint
- `.behavior/v2026_05_04.fix-vars/3.1.3.research.internal.product.code.prod._.v1.i1.md` — prod codepath research
- `.behavior/v2026_05_04.fix-vars/3.1.3.research.internal.product.code.test._.v1.i1.md` — test codepath research

### steps

- [ ] **step 0.1**: modify `replaceProjectVariablesInDeclaredFileContents.ts`
  - [ ] add `{ safe: true }` to flatten call (line 27)
  - [ ] change type to `Record<string, string | string[]>` (line 26)
  - [ ] add array detection and serialization before replaceAll (line 48)
  - **verify**: file compiles without type errors

- [ ] **step 0.2**: add test case to `replaceProjectVariablesInDeclaredFileContents.test.ts`
  - [ ] add primary test: array variables replaced with JSON array literals
  - [ ] add edge test: empty array outputs `[]`
  - [ ] add edge test: single item array outputs `["item"]`
  - **verify**: `npm run test:unit -- replaceProjectVariablesInDeclaredFileContents` passes

- [ ] **step 0.3**: run full test suite
  - **verify**: `npm run test` passes (no regressions)

---

## acceptance criteria

| criterion | verification |
|-----------|--------------|
| arrays are preserved through flatten | test passes for array replacement |
| arrays are serialized to JSON | output contains `["a","b"]` format |
| strings still work | extant tests pass |
| nested objects still work | extant tests pass |
| empty arrays work | edge test passes |
| single item arrays work | edge test passes |

---

## dependencies

```
step 0.1 (impl) → step 0.2 (tests) → step 0.3 (full suite)
```

linear dependency. each step depends on the previous.
