# self-review: has-contract-output-variants-snapped (r5)

## step 1: identify public contracts

examined `src/contract/index.ts` — the public contract exports are:

**SDK methods:**
- `executeApply`
- `executeCompile`
- `executePlan`
- `executeValidate`

**CLI commands:**
- `invoke.ts` (check, fix, plan, apply, compile, validate)

## step 2: check if my changes affect public contracts

**what I modified:**
- `replaceProjectVariablesInDeclaredFileContents.ts` — internal transformer

**is this exported from the public contract?**
- no. this function is NOT exported from `src/contract/index.ts`
- it is an internal transformer used by `evaluateProjectAgainstFileCheckDeclaration`

## step 3: trace impact to public contract output

the change enables array variables to be replaced with JSON array literals. this affects the *content* of files that declapract generates, not the *structure* of CLI/SDK output.

**example:**
- before: `@declapract{variable.approvers}` → `[object Object]` (broken)
- after: `@declapract{variable.approvers}` → `["alice","bob"]` (correct)

this change affects:
- file contents generated by `declapract fix`
- file contents generated by `declapract apply`

it does NOT affect:
- CLI stdout/stderr format
- SDK return value structure
- error message format
- help text

## step 4: check for contract-level tests

looked at contract-level test files:
- `src/contract/sdk/apply.test.ts`
- `src/contract/sdk/plan.test.ts`
- `src/contract/sdk/validate.test.ts`
- `src/contract/sdk/compile.test.ts`

these tests do not use array variables in their fixtures. my change is additive — it enables new behavior (array support) without change to behavior for string variables.

## verdict

**not applicable.** my changes are internal — the function is not part of the public contract. the CLI/SDK output structure is unchanged. the change enables new behavior (array support) without modification to contract output format.

no new contract-level snapshots are needed because:
1. the function is internal, not exported
2. contract output structure is unchanged
3. the change is additive (new feature), not a break
