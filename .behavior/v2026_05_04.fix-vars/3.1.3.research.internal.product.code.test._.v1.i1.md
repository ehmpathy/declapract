# research: test codepaths for array variable support

## pattern 1: unit test structure

**location:** `src/domain.operations/usage/evaluate/projectVariableExpressions/replaceProjectVariablesInDeclaredFileContents.test.ts`

**citation [1]:**
```typescript
describe('replaceProjectVariablesInDeclaredFileContents', () => {
  it('should replace all occurrences of declared variables with their implemented values', async () => {
    const replacedFileContents = replaceProjectVariablesInDeclaredFileContents({
      projectVariables: {
        serviceName: 'super-cool-service',
        organizationName: 'org-of-coolness',
        databaseUserName: { serviceUser: 'super-cool-service-user' },
      },
      fileContents: exampleReadmeFileContents,
    });
    expect(replacedFileContents).toEqual(/* expected output */);
  });
});
```

**relation to wish:** test already covers nested objects via `databaseUserName.serviceUser`. we add a test for arrays.

**action:** [EXTEND] — add test case for array variable replacement.

---

## pattern 2: test fixture template

**location:** `src/domain.operations/usage/evaluate/projectVariableExpressions/replaceProjectVariablesInDeclaredFileContents.test.ts`

**citation [2]:**
```typescript
const exampleReadmeFileContents = `
# @declapract{variable.serviceName}

this is the readme for @declapract{variable.serviceName} of the @declapract{variable.organizationName} org

the database service user is named '@declapract{variable.databaseUserName.serviceUser}'. you can auth under that in dev
`.trim();
```

**relation to wish:** fixture demonstrates variable expression syntax. we add array variable expressions.

**action:** [EXTEND] — add fixture with array variable expressions.

---

## pattern 3: error handling test

**location:** `src/domain.operations/usage/evaluate/projectVariableExpressions/replaceProjectVariablesInDeclaredFileContents.test.ts`

**citation [3]:**
```typescript
it('should throw an error if one of the variables did not have its value defined', async () => {
  try {
    replaceProjectVariablesInDeclaredFileContents({
      projectVariables: { organizationName: 'org-of-coolness' },
      fileContents: exampleReadmeFileContents,
    });
    fail('should not reach here');
  } catch (error) {
    expect(error).toBeInstanceOf(UserInputError);
  }
});
```

**relation to wish:** tests the undefined variable error path. arrays do not change this behavior.

**action:** [REUSE] — no changes needed. error behavior is unchanged.

---

## proposed test case for arrays

**new test to add:**

```typescript
const exampleResourcesFileContents = `
reviewers: { users: @declapract{variable.productionApprovers}, teams: null },
origins: @declapract{variable.allowedOrigins},
`.trim();

it('should replace array variables with JSON array literals', async () => {
  const replacedFileContents = replaceProjectVariablesInDeclaredFileContents({
    projectVariables: {
      productionApprovers: ['alice', 'bob'],
      allowedOrigins: ['https://example.com', 'https://api.example.com'],
    },
    fileContents: exampleResourcesFileContents,
  });
  expect(replacedFileContents).toEqual(
    `
reviewers: { users: ["alice","bob"], teams: null },
origins: ["https://example.com","https://api.example.com"],
    `.trim(),
  );
});
```

**coverage:** tests that:
1. arrays pass through `flatten({ safe: true })` intact
2. `Array.isArray()` detection works
3. `JSON.stringify()` outputs valid JSON array syntax
4. multiple array variables in same file work

---

## additional test cases to consider

| case | input | expected output |
|------|-------|-----------------|
| empty array | `approvers: []` | `[]` |
| single item array | `approvers: ['alice']` | `["alice"]` |
| array of numbers | `ports: [80, 443]` | `[80,443]` |
| mixed string and nested object | `serviceName: 'svc'`, `approvers: ['a']` | both replaced correctly |

---

## summary

| pattern | file | action |
|---------|------|--------|
| unit test structure | replaceProjectVariablesInDeclaredFileContents.test.ts | [EXTEND] |
| test fixture template | replaceProjectVariablesInDeclaredFileContents.test.ts | [EXTEND] |
| error handling test | replaceProjectVariablesInDeclaredFileContents.test.ts | [REUSE] |

**the test changes are localized to one file:** `replaceProjectVariablesInDeclaredFileContents.test.ts`

we add:
1. new fixture with array variable expressions
2. new test case for array replacement
3. optional: edge case tests for empty/single-item arrays
