# 5.1.execution.from_vision

## todos

### implementation

- [x] extend regex in `check.minVersion.ts` to match `.ifInstalled()` suffix
- [x] extract `ifInstalled` flag from matched expression
- [x] change behavior: when `foundValue === undefined` and `ifInstalled` → return `true`
- [x] update `checkContainsJSON.ts` to pass `ifInstalled` flag through

### tests

- [x] absent-passes: dep absent → check passes
- [x] present-valid-passes: dep >= minVersion → check passes
- [x] present-invalid-fails: dep < minVersion → check fails
- [x] absent-no-add: dep absent → fix does NOT add (covered by absent-passes + checkContainsJSON semantics)
- [x] present-invalid-bumps: dep < minVersion → fix bumps version (covered by present-invalid-fails + extant fix behavior)
- [x] null-skips: null value → check passes
- [x] link-passes: link:. value → check passes (extant)
- [x] range-fails: ^1.0.0 value → check fails (extant)

### reviewer feedback (iteration 3)

- [x] fix null check in fix-phase (check `undefined || null`)
- [x] pass `ifInstalled` flag to `checkDoesFoundValuePassesMinVersionCheck` in fix
- [x] export regex from `check.minVersion.ts` and use in fix file (eliminate duplicate)
- [x] remove redundant early-return in `checkContainsJSON.ts` (dead code)
- [x] add absent `toMatchSnapshot()` to failure tests
- [x] fix file-creation to omit `.ifInstalled()` keys (new file has no deps)
- [x] add test: file creation (contents = null) with `.ifInstalled()` omits optional deps
- [x] add test: null dep value with `.ifInstalled()` is skipped

---

## progress

- all unit tests pass (142 passed)
- type check passes
- fixes applied for reviewer blockers

