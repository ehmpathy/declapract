🦉 needs your talons
   ├─ logs: .log/bhrain/review/2026-06-11T05-44-00-592Z
   ├─ review: .behavior/v2026_06_10.fix-minversion/.reviews/5.1.execution.from_vision.peer-review.mech-failhides.md
   └─ summary
      ├─ 1 blockers 🔴
      └─ 0 nitpicks

---
# blocker.1: UnexpectedCodePathError thrown without context/metadata

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.prod/pitofsuccess.errors/rule.require.failloud.md

**locations**:
- src/domain.operations/declaration/readPracticeDeclarations/readPracticeDeclaration/getFileCheckDeclaration/fixMethods/fixContainsJSONByReplacingAndAddingKeyValues.ts

The code throws an UnexpectedCodePathError for an internal invariant violation but provides only a message string with no second argument for context/metadata. Per the rule, errors must use proper classes (like UnexpectedCodePathError for server-side issues) WITH full context objects (e.g. relevant variables, desiredValue, etc.) to enable immediate diagnosis without debug sessions, distinguish who must fix, and aid observability. This is a blocker because missing context violates the failloud requirement and reduces debuggability. The throw must be updated to include a context object, similar to the other UnexpectedCodePathError in the same codebase which correctly uses { declaredValue }.

**snippet**:
```ts
          if (!minVersion)
            throw new UnexpectedCodePathError(
              "checked that its a min version expression but couldn't extract a min version",
            ); // fail fast if weird error occurs
```
