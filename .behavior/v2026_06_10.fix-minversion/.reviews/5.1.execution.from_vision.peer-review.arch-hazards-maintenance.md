🦉 needs your talons
   ├─ logs: .log/bhrain/review/2026-06-11T05-48-17-985Z
   ├─ review: .behavior/v2026_06_10.fix-minversion/.reviews/5.1.execution.from_vision.peer-review.arch-hazards-maintenance.md
   └─ summary
      ├─ 3 blockers 🔴
      └─ 2 nitpicks 🟠

---
# blocker.1: immutability violation: object mutation via assignment

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.forbid.maintenance-hazards.md

**locations**:
- src/domain.operations/declaration/readPracticeDeclarations/readPracticeDeclaration/getFileCheckDeclaration/checkMethods/composableActions/checkContainsJSON.ts:67
- src/domain.operations/declaration/readPracticeDeclarations/readPracticeDeclaration/getFileCheckDeclaration/checkMethods/composableActions/checkContainsJSON.ts:100
- src/domain.operations/declaration/readPracticeDeclarations/readPracticeDeclaration/getFileCheckDeclaration/fixMethods/fixContainsJSONByReplacingAndAddingKeyValues.ts:128

Code mutates objects after const declaration using direct key assignment in loops instead of const + spread/clone. This creates shared mutable state, violates the immutability requirement, and is a maintenance hazard that can lead to hard-to-debug side effects during retries or concurrent execution. Refactor to use immutable patterns like reduce or object spread to build new objects.

**snippet**:
```ts
const declaredToCheckContains: Record<string, any> = {};

// evaluate each key one at a time, to preserve order
const keysToEvaluate = Object.keys(declared);
for (const key of keysToEvaluate) {
  const declaredValue = declared[key];
  const foundValue = found[key];

  // evaluate the value; replace if needed
  const evaluatedValue = (() => {
    ...
  })();

  // set the evaluated value in the declaredToCheckContains object
  declaredToCheckContains[key] = evaluatedValue;
}
```

---

# blocker.2: immutability violation: mutation of shared global regex state

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.forbid.maintenance-hazards.md

**locations**:
- src/domain.operations/declaration/readPracticeDeclarations/readPracticeDeclaration/getFileCheckDeclaration/fixMethods/fixContainsJSONByReplacingAndAddingKeyValues.ts:68

Direct mutation of CHECK_MIN_VERSION_REGEX_GLOBAL.lastIndex = 0 before replace. Global regex objects with /g flag carry mutable lastIndex state which can cause race conditions, incorrect matches on retry, or silent defects in other call sites. This is a maintenance hazard. Use a fresh regex or non-stateful methods like matchAll or replace with a local regex.

**snippet**:
```ts
// reset global regex state and replace remaining check expressions
CHECK_MIN_VERSION_REGEX_GLOBAL.lastIndex = 0;
return filteredString.replace(CHECK_MIN_VERSION_REGEX_GLOBAL, '$1');
```

---

# blocker.3: failloud violation: error without actionable context/metadata

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.forbid.maintenance-hazards.md

**locations**:
- src/domain.operations/declaration/readPracticeDeclarations/readPracticeDeclaration/getFileCheckDeclaration/fixMethods/fixContainsJSONByReplacingAndAddingKeyValues.ts:115

UnexpectedCodePathError thrown without metadata/context object containing relevant inputs or debug info. Per failloud, errors must include what failed, why, how to fix, and relevant input values. Generic message without metadata hides root cause and increases debug hours. Add metadata like the input value or expression.

**snippet**:
```ts
if (!minVersion)
  throw new UnexpectedCodePathError(
    "checked that its a min version expression but couldn't extract a min version",
  ); // fail fast if weird error occurs
```

---

# nitpick.1: narrative flow: complex IIFE + for-loop mutation instead of linear early returns

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.forbid.maintenance-hazards.md

**locations**:
- src/domain.operations/declaration/readPracticeDeclarations/readPracticeDeclaration/getFileCheckDeclaration/checkMethods/composableActions/checkContainsJSON.ts:52

Logic uses nested IIFEs inside for loops with early returns to simulate flow, plus object mutation. While no explicit 'else', this creates non-obvious paths that are hard to scan. Prefer flat paragraphs with early returns/throws and immutable transforms for clearer narrative per the rule.

**snippet**:
```ts
const evaluatedValue = (() => {
  // handle case where declared value is falsy
  if (!declaredValue) return declaredValue;

  // handle cases where the declared value is itself an object (i.e., go one layer deeper, recursively)
  if (typeof declaredValue === 'object')
    return recursivelyEvaluateDeclaredContentsToCheckContains({
      declared: declaredValue,
      found: foundValue,
    });

  // handle cases where the declared value is a check reference to evaluate
  if (
    typeof declaredValue === 'string' &&
    declaredValue.startsWith('@declapract{check.')
  ) {
    if (isCheckMinVersionExpression(declaredValue)) {
      const evaluatedValue =
        evaluateSupportedJSONCheckExpressionMinimumVersion({
          declaredValue,
          foundValue,
        });
      return evaluatedValue;
    }
  }

  // if did not match any of the above, then its just the declared value still
  return declaredValue;
})();
```

---

# nitpick.2: immutability/narrative: let mutation in test setup

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.forbid.maintenance-hazards.md

**locations**:
- src/domain.operations/declaration/readPracticeDeclarations/readPracticeDeclaration/getFileCheckDeclaration/fixMethods/fixContainsJSONByReplacingAndAddingKeyValues.test.ts:165

Use of let for consoleSpy with re-assignment in beforeEach/afterEach. While in test, still mutates state and uses let. Per immutability rule, avoid let/var except in explicitly annotated mutation blocks. Use const + jest's built-in cleanup or beforeEach return for spies.

**snippet**:
```ts
let consoleSpy: jest.SpyInstance;

beforeEach(() => {
  consoleSpy = jest.spyOn(console, 'log').mockImplementation();
});

afterEach(() => {
  consoleSpy.mockRestore();
});
```
