today, this cmd depends on the tsconfig of its host

ideally, we can run this without any tsconfig at all - since we simply want to execute the ts code, not typecheck it

that way, when we're in the middle of upgrading the host project's tsconfig, retry of apply wont fail
