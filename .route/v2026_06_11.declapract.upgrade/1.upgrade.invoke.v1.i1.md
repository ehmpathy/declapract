# 1.upgrade.invoke outcome

## status

upgrade completed successfully.

## fixed during upgrade

biome lint errors required manual fixes before upgrade could complete:

1. **node.js import protocol** - added `node:` prefix to builtin imports:
   - `src/contract/cli/invoke.ts`: `'fs'` -> `'node:fs'`, `'path'` -> `'node:path'`
   - `src/utils/wrappers/withDurationReporting.ts`: `'process'` -> `'node:process'`

2. **shadowed global** - renamed variable that shadowed `toString`:
   - `src/utils/stringPolyfill/replaceAll.ts`: `toString` -> `replacement`

## warnings (not fixed)

the following biome warnings remain (not errors, so not blocking):
- `useExplicitType` - many functions lack explicit return types
- `noExplicitAny` - some `any` types used

these are pre-existing and outside the scope of this upgrade.

## notes

- declapract-typescript-ehmpathy upgraded from 0.47.63 to 0.47.75
- test-fns upgraded from 1.4.2 to 1.15.8
- declastruct upgraded from 1.7.3 to 1.9.1
- 7 roles linked and initialized