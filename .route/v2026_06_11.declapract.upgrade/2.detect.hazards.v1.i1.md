# 2.detect.hazards

## test hazards

### jest config changes
- **slowtest reporter added** to both unit and integration configs
  - new reporter: `test-fns/slowtest.reporter.jest`
  - outputs to `.log/slowtest/{unit,integration}.report.json`
  - verify: `rhx git.repo.test --what unit --mode apply`

### test-fns upgrade (1.4.2 -> 1.15.8)
- major version jump, may have breaking changes in test utilities
- verify: run all tests

### test script changes
- scripts now use `set -eu` for strict error handling
- THOROUGH, RESNAP, CI flags changed syntax
- verify: `npm run test:unit` and `npm run test:integration`

### @jest/globals added
- new devDependency for jest 30.2.0 compatibility
- may require imports to use `@jest/globals` pattern

## cicd hazards

### cycle detection added (RESOLVED)
- new `test:lint:cycles` command using dpdm
- `.dpdmrc.yaml` added with empty exclude list
- **src cycle fixed**: `src/domain.objects/FileCheckDeclarationInput.ts` was importing from barrel `./` instead of `./FileCheckDeclaration`
- **node_modules cycles resolved**:
  - `domain-objects` upgraded to 0.31.13 (no cycles)
  - `joi` replaced with `zod@4.3.4` (no cycles)
  - `semver` replaced with inline implementation (no cycles)
  - `yaml` removed (was transitive dep from joi)
  - `expect` cycles remain but jest is dev-only dependency
- ✅ lint passes

### test scripts use strict mode
- `set -eu` added to test scripts
- unset variables or failing subcommands will fail the build
- verify: run tests in CI environment

## config hazards

### biome.jsonc changes
- **noShadowRestrictedNames removed** - was warn, now off
- **noThisInStatic added** - warn without autofix
- verify: `npm run test:lint:biome`

### build scripts support bun
- new `build:clean:bun` and `build:compile:bun` scripts
- only runs if present (`--if-present`)
- should not affect existing builds

## path hazards

### test asset directories (RESOLVED)
- declapract upgrade incorrectly renamed test fixture files
- these fixtures are meant to be "bad" examples that fail validation
- **fix**: restored test assets to original locations
- ✅ integration tests pass

### deleted files
- `rhachet.use.ts` deleted
- `.agent/repo=.this/role=any/skills/use.apikeys.sh` deleted
- verify: no imports reference deleted files

## verification plan

1. run lint:cycles to check for circular dependencies
2. run unit tests
3. run integration tests
4. verify build compiles